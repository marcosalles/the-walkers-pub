# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

# Home page
GET		/										controllers.MainController.home()
GET		/auth									controllers.MainController.authenticated()
GET		/signup									controllers.PlaneswalkerUserController.signUpLink()
POST	/signup									controllers.PlaneswalkerUserController.signUp()
POST	/login									controllers.PlaneswalkerUserController.login()

# Map static resources from the /public folder to the /assets URL path
GET		/assets/*file							controllers.Assets.at(path="/public", file)
GET		/webjars/*file							controllers.WebJarAssets.at(file)
